# Render Blueprint Specification for MPF
# https://render.com/docs/blueprint-spec

services:
  # 1. Backend FastAPI Service
  - type: web
    name: mpf-backend
    runtime: docker
    dockerfilePath: backend/Dockerfile
    dockerContext: backend
    dockerCommand: sh -c "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: MPF_DATABASE_URL
        fromDatabase:
          name: mpf-postgres
          property: connectionString
      - key: MPF_REDIS_URL
        fromService:
          type: redis
          name: mpf-redis
          property: connectionString
      - key: MPF_STORAGE_ROOT
        value: /data/storage
      - key: MPF_CORS_ORIGINS
        value: '["*"]'
    healthCheckPath: /health

  # 2. Celery Worker Service
  - type: worker
    name: mpf-worker
    runtime: docker
    dockerfilePath: backend/Dockerfile
    dockerContext: backend
    dockerCommand: celery -A app.workers.celery_app.celery worker --loglevel=info --concurrency=2
    envVars:
      - key: MPF_DATABASE_URL
        fromDatabase:
          name: mpf-postgres
          property: connectionString
      - key: MPF_REDIS_URL
        fromService:
          type: redis
          name: mpf-redis
          property: connectionString
      - key: MPF_STORAGE_ROOT
        value: /data/storage

  # 3. Frontend Static Site
  - type: web
    name: mpf-frontend
    runtime: static
    buildCommand: cd frontend && npm install && npm run build
    staticPublishPath: ./frontend/dist
    envVars:
      - key: VITE_API_BASE_URL
        fromService:
          type: web
          name: mpf-backend
          property: host
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

databases:
  - name: mpf-postgres
    databaseName: mpf
    user: mpf

services:
  # Redis key-value store for Celery task broker
  - type: redis
    name: mpf-redis
    ipAllowList: []
